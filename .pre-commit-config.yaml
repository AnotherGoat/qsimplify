repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.6
    hooks:
      - id: ruff
        name: Ruff isort
        args: [--fix, --select=I]
        stages: [pre-commit]
      - id: ruff-format
        name: Ruff format
        stages: [pre-commit]
